import 'package:flutter/material.dart';

class NewEventFormWidget extends StatelessWidget {
  const NewEventFormWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
